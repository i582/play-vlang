/**
 * Base class for all code sources for the Playground.
 */
class CodeRepository {
    /**
     * Saves the code to the storage.
     * @param code
     */
    saveCode(code) {}

    /**
     * Gets the code from the storage.
     */
    getCode(onReady) {}
}
